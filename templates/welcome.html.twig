<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tusker PHP Framework</title>
        <style>
            html,
            body {
                padding: 0;
                margin: 0;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            }

            @media (prefers-color-scheme: light) {
                body {
                    background: #FFF;
                    color: #606060;
                }
            }

            @media (prefers-color-scheme: dark) {
                body {
                    background: #333;
                    color: #FFF;
                }
            }
            .body{
                display: flex;
                height: calc(100vh - 60px);
                justify-content: center;
                align-items: center;
                font-size: 50px;
                font-weight: 100;
                text-transform: uppercase;
            }
            .body p.title {
                margin: 20px 0px;
            }
            .body span.small {
                font-size: 20px;
                text-align: center;
                display: block;
            }
            .footer {
                display: flex;
                height: 60px;
                justify-content: center;
                align-items: center;
                font-size: 18px;
                font-weight: 100;
                background: #f5f5f5;
                color: #606060
            }
        </style>
    </head>

    <body>
        <section class="body">
            <div>
                <p class="title">
                    Tusker PHP Framework
                </p>
                <span class="small">version {{ app_version() }}</span>
            </div>
        </section>
        <footer class="footer">
            All rights reserved Tusker &copy; 2023
        </footer>
    </body>
</html>